<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :steam_nickname %><br />
    <%= f.text_field :steam_nickname %>
  </div>
  <div class="field">
    <%= f.label :steam_id %><br />
    <%= f.number_field :steam_id %>
  </div>
  <div class="field">
    <%= f.label :steam_image %><br />
    <%= f.text_field :steam_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
